# 不装 Xcode，云端编译 Mac 版「采购管家」图文教程

全程免费，约 20–30 分钟（其中编译等待约 10–15 分钟，无需守着）。原理：把工程上传到 GitHub，由它免费提供的云端 Mac 自动编译，最后下载编译好的 App 即可。

> 适用：在 Mac 上使用。iPhone 版因苹果限制必须通过本机 Xcode 签名安装，云端无法直接产出可装到手机的安装包；以后有条件装 Xcode 时，同一个工程直接编译手机版即可。

---

## 第 1 步：注册 GitHub 账号

1. 浏览器打开 **github.com/signup**，用邮箱注册（用户名、密码按提示完成，免费套餐即可）。
2. 邮箱里收一封验证邮件，点链接完成验证。
3. 免费套餐的问题（几个人使用、是否学生/教师等）随意选，最后选 **Free（免费）** 方案。

## 第 2 步：新建一个代码仓库

1. 登录后，点右上角 **「+」→ New repository**。
2. 填写：
   - **Repository name**：`procurement-tracker`（随意，英文即可）；
   - 选择 **Private（私有）**（代码只有你自己可见）；
   - **“Add a README file”、“.gitignore”、“license”三个选项都不要勾选**（保持空仓库，避免上传冲突）。
3. 点绿色按钮 **Create repository**。

## 第 3 步：上传工程文件（关键：要包含隐藏的 .github 文件夹）

1. 在 Mac 上解压我给你的**最新版**压缩包（文件名含“云端版”或压缩包内有 `.github`、`scripts` 文件夹的那个），打开解压出的文件夹，应能看到：
   - `.github`（云端编译配置，默认是隐藏的）
   - `ProcurementTracker`（工程本体）
   - `scripts`
   - `README.md` 等
2. 在该文件夹窗口按快捷键 **`Cmd + Shift + .`（句号）**，让隐藏文件显示出来——此时应能看到灰色的 **`.github`** 文件夹。
3. 按 **`Cmd + A`** 全选该文件夹里的所有内容（含 `.github`）。
4. 回到 GitHub 刚建好的空仓库网页：点 **「uploading an existing file」** 链接（或页面里的 **Add file → Upload files**）。
5. 把第 3 步全选的所有文件/文件夹**一起拖进网页的拖拽区**，等待列表全部上传完成（页面底部会显示每个文件的进度）。
   - 必须确认文件列表里能看到 **`.github/workflows/build-mac.yml`**，少了它云端不会编译。
   - 推荐用 **Chrome 或 Edge** 浏览器；Safari 对文件夹上传支持较差。
6. 拉到页面底部，点绿色按钮 **Commit changes**。

## 第 4 步：等待云端编译

1. 点仓库页面顶部的 **Actions（行动）** 标签。
2. 第一次进入会出现一条提示，点 **「I understand my workflows, go ahead and enable them」** 启用。
3. 列表中会出现一条名为 **Build ProcurementTracker for macOS** 的记录，前面是**黄色转圈**表示正在编译（约 10–15 分钟，可关掉网页去做别的事，完成后结果会保留）。
4. 编译成功：变成**绿色对勾**。
   编译失败：变成**红叉**。点进该记录 → 点左侧 **Build macOS App** → 展开带红叉的步骤，把报错截图发我，我来修配置后你重新上传即可。
   - 也可以在 Actions 页面点 workflow 名 → 右侧 **Run workflow** 手动重新触发编译。

## 第 5 步：下载编译好的 App

1. 点进那条**绿色对勾**的编译记录。
2. 页面拉到最底部 **Artifacts（产物）** 区域，点击 **ProcurementTracker-Mac** 下载（是一个压缩包，Safari 可能提示“此网站正在下载”，允许即可）。

## 第 6 步：在 Mac 上首次打开

1. 下载得到 `ProcurementTracker-Mac.zip`，**双击解压**，里面是 `ProcurementTracker-App.zip`，**再双击解压**，得到两个东西：
   - `ProcurementTracker.app`
   - `首次打开.command`
2. **右键点「首次打开.command」→ 选“打开”**（必须右键，不要双击），弹窗里再点一次 **“打开”**。
   - 它会自动解除 macOS 的安全隔离、把 App 复制进“应用程序”文件夹并启动。
   - 过程中若终端询问是否允许访问文件夹 / 通知 / 日历，点允许。
3. 以后直接从 **启动台（Launchpad）** 或“应用程序”里打开“采购管家”即可，不用再运行脚本。

### 如果右键 .command 仍提示无法打开（备用方法）

1. 把 `ProcurementTracker.app` 手动拖进“应用程序”文件夹。
2. 打开“终端”（启动台搜“终端”/Terminal），粘贴下面这一行后回车：

   ```bash
   xattr -dr com.apple.quarantine /Applications/ProcurementTracker.app && open /Applications/ProcurementTracker.app
   ```

## 第 7 步（可选）：以后修改了代码怎么重新编译

- 工程有更新时，在 GitHub 仓库页 **Add file → Upload files**，把新文件拖上去（同名文件会自动覆盖），Commit 后云端会自动重新编译，再到 Actions 下载新产物即可。
- 免费私有账户每月包含约 200 分钟的 Mac 编译时长（每次约 10–15 分钟，够用十几次）；用完后次月 1 号重置。

---

## App 内使用提示

- 首次启动建议在「设置」里授权**通知**和**系统日历**；飞书日历同步属于可选项，需要先按 `README.md` 第四节创建飞书自建应用，再把 App ID / Secret 填进设置页。
- 云端编译的是 ad-hoc 签名版本：飞书登录态、App Secret 保存在本机钥匙串中；若以后换用不同方式重新编译安装，可能需要重新填写一次飞书配置，采购数据不受影响（也可用设置页的 JSON 导出做备份）。
